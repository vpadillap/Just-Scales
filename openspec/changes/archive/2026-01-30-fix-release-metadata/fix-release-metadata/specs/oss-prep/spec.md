# Specification: Open Source Standards

## Requirement: Release Metadata
The project build configuration MUST explicitly state:
-   `author`: The primary developer or organization.
-   `copyright`: Valid copyright string.
-   `publisherName`: (Windows) The organization name shown in permissions dialogs (note: allows overriding default inference).
