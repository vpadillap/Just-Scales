## ADDED Requirements

### Requirement: Functional Tooltips on Android
Tooltips, specifically "view instructions", must be interactable on Android devices via tap.

#### Scenario: Tap to View Instructions
- **WHEN** user taps "view instructions" tooltip on an Android device
- **THEN** the tooltip opens and displays the content
- **AND** the tooltip does not immediately close or fail to render
